# Backend API URL — required for production builds (S3/CloudFront deployment).
# Set this to your backend's public URL before running `npm run build`.
# Leave unset for local dev (the Vite dev server proxies /api → http://localhost:8000).
VITE_API_URL=https://api.your-domain.com
